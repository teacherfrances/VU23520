<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Learning Activities Dashboard</title>
    <style>
        /* General Page Setup */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f6f8;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
        }

        /* Deep Navy Header */
        header {
            background-color: #0A192F; /* Deep Navy Blue */
            color: #FFFFFF; /* White Text */
            width: 100%;
            padding: 20px 0;
            text-align: center;
            position: relative;
            box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }

        /* Muted Gold Lesson Accent */
        .lesson-number {
            color: #D4AF37; /* Muted Gold */
            font-weight: bold;
            font-size: 14px;
            position: absolute;
            left: 30px;
            top: 25px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        header h1 {
            margin: 0;
            font-size: 28px;
        }

        /* Layout Grid for Split Screen */
        .dashboard-layout {
            display: grid;
            grid-template-columns: 320px 1fr;
            gap: 25px;
            width: 95%;
            max-width: 1400px;
            margin: 30px 0;
            box-sizing: border-box;
        }

        /* Left Side: Sidebar Control Panel */
        .sidebar {
            background-color: #FFFFFF; /* White Content Background */
            border: 1px solid #D1D5DB; /* Light Gray Border */
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
            height: fit-content;
        }

        /* Pale Yellow Learning Box */
        .learning-box {
            background-color: #FEF9E7; /* Pale Yellow / Cream */
            border-left: 5px solid #D4AF37; /* Muted Gold Accent Bar */
            padding: 15px;
            border-radius: 6px;
            margin-bottom: 25px;
        }

        .learning-box h2 {
            margin: 0 0 5px 0;
            color: #1F2937; /* Dark Gray */
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .learning-box p {
            margin: 0;
            color: #4B5563; /* Main Body Text */
            font-size: 14px;
            line-height: 1.4;
        }

        /* Links / Buttons Stack */
        .links-stack {
            display: flex;
            flex-direction: column;
            gap: 12px;
            margin-bottom: 20px;
        }

        /* Teal Action Buttons */
        .btn-start {
            display: block;
            background-color: #008080; /* Teal / Dark Green */
            color: #FFFFFF; /* White Text */
            text-decoration: none;
            text-align: center;
            padding: 14px;
            border-radius: 8px;
            font-size: 16px;
            font-weight: bold;
            transition: transform 0.2s, background-color 0.2s;
            box-shadow: 0 4px 10px rgba(0, 128, 128, 0.15);
        }

        .btn-start:hover {
            background-color: #006666; /* Darker Teal */
            transform: translateY(-2px);
        }

        /* White Restart Button */
        .btn-restart {
            display: block;
            background-color: #FFFFFF; /* White Background */
            color: #1F2937; /* Dark Gray Text */
            text-decoration: none;
            text-align: center;
            padding: 12px;
            border-radius: 8px;
            font-size: 14px;
            font-weight: bold;
            border: 2px solid #D1D5DB; /* Light Gray Border */
            transition: transform 0.2s, background-color 0.2s;
        }

        .btn-restart:hover {
            background-color: #F9FAFB; /* Subtle off-white */
            transform: translateY(-2px);
        }

        .btn-start:active, .btn-restart:active {
            transform: scale(0.98);
        }

        /* Right Side: Activity Frame Window */
        .activity-display {
            background-color: #FFFFFF; /* White Background */
            border: 1px solid #D1D5DB; /* Light Gray Border */
            border-radius: 12px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
            overflow: hidden;
            display: flex;
            flex-direction: column;
            min-height: 600px;
        }

        /* Default Placeholder Screen View */
        .frame-placeholder {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            flex: 1;
            color: #4B5563; /* Dark Gray Text */
            text-align: center;
            padding: 40px;
        }

        .frame-placeholder svg {
            fill: #4682B4; /* Steel Blue Icon Accent */
            margin-bottom: 15px;
        }

        /* Hidden Frame Window till needed */
        iframe {
            width: 100%;
            height: 100%;
            border: none;
            flex: 1;
            display: none;
        }
    </style>
</head>
<body>

    <!-- Header Section -->
    <header>
        <span class="lesson-number">Lesson 01</span>
        <h1>Course Dashboard</h1>
    </header>

    <!-- Main Dynamic Layout -->
    <div class="dashboard-layout">
        
        <!-- Left Column: Controls -->
        <div class="sidebar">
            <div class="learning-box">
                <h2>Today I am learning to:</h2>
                <p>Complete the core sequence modules step-by-step using interactive elements.</p>
            </div>

            <!-- Interactive Navigation Target Stack -->
            <div class="links-stack">
                <!-- target="activity-window" directs links to load inside our iframe -->
                <a href="activity1.html" target="activity-window" class="btn-start" onclick="showFrame()">▶ Activity One</a>
                <a href="activity2.html" target="activity-window" class="btn-start" onclick="showFrame()">▶ Activity Two</a>
                <a href="activity3.html" target="activity-window" class="btn-start" onclick="showFrame()">▶ Activity Three</a>
                <a href="activity4.html" target="activity-window" class="btn-start" onclick="showFrame()">▶ Activity Four</a>
            </div>

            <!-- System Control Button -->
            <a href="javascript:location.reload()" class="btn-restart">🔄 Reset System View</a>
        </div>

        <!-- Right Column: Interactive Content Window -->
        <div class="activity-display">
            <!-- Screen shown before clicking buttons -->
            <div id="placeholder" class="frame-placeholder">
                <svg width="64" height="64" viewBox="0 0 24 24"><path d="M19 3H5c-1.1 0-2 .9-2 2v14c0 1.1.9 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm-1 16H6c-.55 0-1-.45-1-1V6c0-.55.45-1 1-1h12c.55 0 1 .45 1 1v12c0 .55-.45 1-1 1zm-4.5-3.5L11 12l-2.5 3.5h7z"/></svg>
                <h3>System Ready</h3>
                <p>Select an activity button on the left to launch the interactive workspace module here.</p>
            </div>

            <!-- Content frame container -->
            <iframe id="activity-window" name="activity-window"></iframe>
        </div>

    </div>

    <!-- Interface Controller Logic Script -->
    <script>
        function showFrame() {
            document.getElementById('placeholder').style.display = 'none';
            document.getElementById('activity-window').style.display = 'block';
        }
    </script>

</body>
</html>
